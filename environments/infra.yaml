name: infrastructure_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - azure-ai-ml
      - azure-identity
      - azureml-core
      - azureml-sdk
      - azureml-defaults
      - azureml-widgets
      - azureml-tensorboard
      - azureml-mlflow
      - pydantic
      - python-dotenv
